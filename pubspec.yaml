name: lyre
description: A sample command-line application.
# version: 1.0.0

environment:
  sdk: ^3.8.0

dependencies:
  cli_annotations: ^0.1.0
  http: ^1.4.0
  kuroshiro: ^1.0.0

dev_dependencies:
  build_runner: ^2.5.4
  cli_gen: ^0.1.0
  lints: ^5.0.0
  test: ^1.24.0
